#include <bits/stdc++.h>
#include "./helper.h"
using namespace std;

int main() {
    setIO("/home/uggi/projects/DSA/CSES/tests/AdvancedTechniques/ReachabilityQueries2143/1.in") ;
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // CSES Problem: ReachabilityQueries (2143)
    // Reads from stdin, writes to stdout
    // Sample usage for debugging against test files:
    // freopen("input.in", "r", stdin);
    // freopen("output.out", "w", stdout);

    int n;
    cin >> n;
    cout << n << "\n";

    return 0;
}
